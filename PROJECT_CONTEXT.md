# 校园志愿活动报名与候补协同平台：项目交接说明

## 1. 项目定位

面向班级、社团和校园志愿组织的活动报名、候补、签到与通知协同平台。

项目源于真实场景：活动通常发布在 QQ 群中，消息容易被淹没；报名者临时退出时只能私下协调，缺少公开、透明的候补递补机制。

## 2. 已确认的产品方向

- 以适配手机的 Vue 3 Web 页面为主要入口，可通过 QQ 链接分享。
- 支持组织、成员和活动管理。
- 支持先到先得与人工审核两种报名模式。
- 名额已满后进入候补队列；空位出现时按顺序发放限时确认资格。
- 支持主动取消、候补递补、动态二维码签到、人工补签和操作审计。
- 站内提供活动列表、状态提醒和通知，降低对群消息的依赖。
- 第一版不自动处罚取消或缺席，只保留记录，避免规则过重。

## 3. 已确认的技术架构

- 前端：Vue 3 响应式 Web 应用。
- 后端：Spring Boot 模块化单体，先保证可实现、可测试和可部署。
- 数据库：MySQL 作为最终事实来源。
- Redis：用于动态签到码、缓存、限流等增强能力，随项目学习后接入。
- Kafka：用于异步通知等扩展场景，采用事件与 Outbox 思路逐步接入。
- 部署：Docker Compose。

## 4. 第一版核心范围

- 角色：平台管理员、组织负责人、活动管理员、普通成员。
- 活动生命周期：草稿、发布、报名中、报名截止、进行中、已结束、已取消。
- 报名模式：`FIRST_COME` 与 `REVIEW`。
- 候补确认时限可配置为 30 分钟至 12 小时，默认 2 小时。
- 候补邀请生效时暂时保留名额；接受后转为正式报名，拒绝或超时后继续递补。
- 退出候补后再次报名进入队尾。
- 报名表自动带入基础资料，并允许最多增加 5 个问题；活动发布后表单结构不可修改。
- 问题类型：文本、单选、多选、布尔值。
- 签到使用 Redis 中带有效期的动态二维码；异常时允许填写原因后人工补签，并留下审计记录。
- 第一版不做地理围栏。

## 5. 第二版方向：聊天室与 AI

- 每个活动拥有绑定聊天室，区分正式公告与普通讨论；活动归档后只读。
- AI 从聊天内容中提取可能的时间、地点或注意事项变更，但必须由负责人确认后才能成为正式信息。
- AI 可生成未读消息摘要。
- AI 可基于活动规则与正式资料进行带来源的问答。
- AI 无权直接修改活动、报名、候补或签到等正式数据。

## 6. 质量与验收重点

- 用并发测试验证 100 人争抢 20 个名额时不能超卖。
- 重点验证取消、候补邀请、接受、拒绝、超时与继续递补的完整状态链。
- 测试栈可逐步采用 JUnit、Mockito、Testcontainers、Vitest、Playwright 与 k6。
- 计划邀请至少 10 名同学进行真实试用并收集反馈。

## 7. 学习与推进约束

- 开发者是准大二学生，已完成 JavaWeb 课程。
- 9 月开学前每天约可投入 2 小时，开学后每天可投入 5 小时以上。
- Redis、Kafka 等模块尚未系统学习，希望通过项目按需补充，而不是先学完所有中间件再开始。
- 推荐顺序：先完成不依赖中间件的可运行核心闭环，再逐步引入 Redis、Kafka、可观测性和 AI。

## 8. 当前状态与下一步

- 产品与技术规格的八个部分已经确认。
- 正式设计规格和完整 PPT 已复制到本目录。
- 尚未初始化代码工程与 Git 仓库。
- 下次讨论应从“第一阶段实施计划与工程骨架”开始；除非需求发生变化，不必重新讨论已确认的规格。

建议下一步依次确定：模块目录、数据库核心表、第一条端到端用户故事，以及第一周每日两小时的开发任务。
